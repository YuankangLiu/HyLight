Installation
============

To use HyLight, first install it using pip:

.. code-block:: console

   $ pip install hylightpy

.. note::

   The package is called ``hylightpy`` on `PyPI <https://pypi.org/project/hylightpy/>`_, with extra letters ``py`` at the end.
